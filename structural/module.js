// Whenever you're encapsulating a block of code into a singular function or a pure function, as it is sometimes referred to,
// you're creating a module.

const calc = () => {
  return 4 * 3;
};

export default calc;
